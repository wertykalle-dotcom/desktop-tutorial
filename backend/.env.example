DATABASE_PATH=./test.db
DATABASE_URL=
REDIS_URL=redis://redis:6379/0
SECRET_KEY=replace-me-with-a-long-random-secret
MONGO_URL=
DB_NAME=social_media_db
SUPER_ADMIN_EMAIL=test@test.com
EXCHANGE_RATES_API_URL=
EXCHANGE_RATES_REFRESH_SECONDS=86400
MODERATION_PROVIDER_URL=
MODERATION_PROVIDER_API_KEY=
MODERATION_PROVIDER_MODEL=moderation
MODERATION_PROVIDER_KIND=http
GEOIP_LOCALE_PROVIDER_URL=
TURN_REALM=yosla.local
TURN_USER=yosla
TURN_PASSWORD=replace-me-with-a-strong-turn-password
# Optional moderation provider can return a combined schema with:
# - result.score
# - result.action
# - result.queue
# - result.reason
# - result.locale / result.language
# - result.signals
# Set MODERATION_PROVIDER_KIND=openai to use an OpenAI-compatible provider via
# AsyncOpenAI and MODERATION_PROVIDER_URL as the base URL.
